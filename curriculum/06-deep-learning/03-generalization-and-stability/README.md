# 03 — Generalization, Regularization & Training Stability

This subtrack covers the gap between fitting a training set and building a model that behaves reliably.

## Learning units

- bias and variance in deep models
- L1/L2 regularization and weight decay
- dropout
- early stopping
- data augmentation
- label smoothing
- batch normalization
- layer normalization
- group and instance normalization
- residual connections
- hyperparameter search
- class imbalance
- noisy labels
- calibration and confidence
- scaling behavior and model capacity

Every unit should include failure cases and experiments, not only definitions.
